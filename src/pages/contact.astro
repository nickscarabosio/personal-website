---
import PageLayout from "../layouts/PageLayout.astro";
import SectionLabel from "../components/ui/SectionLabel.astro";
---

<PageLayout
  title="Contact — Nick Scarabosio"
  description="Get in touch with Nick Scarabosio. Book a call, start a project, or just say hello."
>
  <section
    class="mx-auto flex min-h-[80vh] max-w-2xl flex-col items-center justify-center px-6 py-20"
  >
    <div id="contact-form-wrapper" class="w-full">
      <div class="mb-10 text-center">
        <SectionLabel class="mb-4">Contact</SectionLabel>
        <h1
          class="font-heading text-[length:var(--text-3xl)] text-[var(--color-text-primary)]"
        >
          Let's talk.
        </h1>
        <p
          class="mt-3 text-[length:var(--text-base)] font-light text-[var(--color-text-secondary)]"
        >
          Have a project, partnership, or idea? Drop a message and I'll get back
          to you.
        </p>
      </div>

      <form
        id="contact-form"
        action="https://formspree.io/f/mjgabqje"
        method="POST"
        class="flex flex-col gap-5"
      >
        <input type="hidden" name="_subject" value="New message from nickscarabosio.com" />

        <div class="grid gap-5 md:grid-cols-2">
          <div class="flex flex-col gap-2">
            <label
              for="firstName"
              class="text-[length:var(--text-xs)] font-medium uppercase tracking-wider text-[var(--color-text-secondary)]"
            >
              First Name
            </label>
            <input
              type="text"
              id="firstName"
              name="firstName"
              required
              autocomplete="given-name"
              class="rounded-lg border border-[var(--color-border)] bg-[var(--color-bg-secondary)] px-4 py-3 text-[length:var(--text-base)] text-[var(--color-text-primary)] outline-none transition-colors duration-300 placeholder:text-[var(--color-text-muted)] focus:border-[var(--color-accent)]"
              placeholder="Nick"
            />
          </div>
          <div class="flex flex-col gap-2">
            <label
              for="lastName"
              class="text-[length:var(--text-xs)] font-medium uppercase tracking-wider text-[var(--color-text-secondary)]"
            >
              Last Name
            </label>
            <input
              type="text"
              id="lastName"
              name="lastName"
              required
              autocomplete="family-name"
              class="rounded-lg border border-[var(--color-border)] bg-[var(--color-bg-secondary)] px-4 py-3 text-[length:var(--text-base)] text-[var(--color-text-primary)] outline-none transition-colors duration-300 placeholder:text-[var(--color-text-muted)] focus:border-[var(--color-accent)]"
              placeholder="Scarabosio"
            />
          </div>
        </div>

        <div class="flex flex-col gap-2">
          <label
            for="email"
            class="text-[length:var(--text-xs)] font-medium uppercase tracking-wider text-[var(--color-text-secondary)]"
          >
            Email
          </label>
          <input
            type="email"
            id="email"
            name="email"
            required
            autocomplete="email"
            class="rounded-lg border border-[var(--color-border)] bg-[var(--color-bg-secondary)] px-4 py-3 text-[length:var(--text-base)] text-[var(--color-text-primary)] outline-none transition-colors duration-300 placeholder:text-[var(--color-text-muted)] focus:border-[var(--color-accent)]"
            placeholder="nick@example.com"
          />
        </div>

        <div class="flex flex-col gap-2">
          <label
            for="message"
            class="text-[length:var(--text-xs)] font-medium uppercase tracking-wider text-[var(--color-text-secondary)]"
          >
            Message
          </label>
          <textarea
            id="message"
            name="message"
            required
            rows="5"
            class="resize-none rounded-lg border border-[var(--color-border)] bg-[var(--color-bg-secondary)] px-4 py-3 text-[length:var(--text-base)] text-[var(--color-text-primary)] outline-none transition-colors duration-300 placeholder:text-[var(--color-text-muted)] focus:border-[var(--color-accent)]"
            placeholder="Tell me about your project or idea..."
          ></textarea>
        </div>

        <button
          type="submit"
          class="mt-2 inline-flex items-center justify-center rounded-full bg-[var(--color-accent)] px-8 py-3.5 text-[length:var(--text-sm)] font-medium uppercase tracking-wider text-[var(--color-bg-primary)] transition-all duration-300 hover:brightness-110 hover:shadow-[0_0_20px_var(--color-accent-dim)]"
        >
          Send a Message
        </button>
      </form>
    </div>

    <!-- Success message (hidden by default) -->
    <div
      id="contact-success"
      class="hidden w-full py-20 text-center"
    >
      <div
        class="mb-4 inline-flex h-16 w-16 items-center justify-center rounded-full border border-[var(--color-accent)] text-[var(--color-accent)]"
      >
        <svg width="28" height="28" viewBox="0 0 24 24" fill="none">
          <path
            d="M5 13l4 4L19 7"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"></path>
        </svg>
      </div>
      <h2
        class="font-heading text-[length:var(--text-2xl)] text-[var(--color-text-primary)]"
      >
        Message sent.
      </h2>
      <p
        class="mt-2 text-[length:var(--text-base)] font-light text-[var(--color-text-secondary)]"
      >
        Thanks for reaching out — I'll get back to you soon.
      </p>
    </div>
  </section>
</PageLayout>

<script>
  import gsap from "gsap";

  function initContact() {
    const form = document.getElementById("contact-form") as HTMLFormElement | null;
    const wrapper = document.getElementById("contact-form-wrapper");
    const success = document.getElementById("contact-success");
    if (!form || !wrapper || !success) return;

    // Entrance animation
    gsap.fromTo(
      wrapper,
      { opacity: 0, y: 30 },
      { opacity: 1, y: 0, duration: 0.8, ease: "power2.out" }
    );

    form.addEventListener("submit", async (e) => {
      e.preventDefault();

      const data = new FormData(form);

      try {
        const res = await fetch(form.action, {
          method: "POST",
          body: data,
          headers: { Accept: "application/json" },
        });

        if (res.ok) {
          gsap.to(wrapper, {
            opacity: 0,
            y: -20,
            duration: 0.4,
            onComplete: () => {
              wrapper.classList.add("hidden");
              success.classList.remove("hidden");
              gsap.fromTo(
                success,
                { opacity: 0, y: 20 },
                { opacity: 1, y: 0, duration: 0.6, ease: "power2.out" }
              );
            },
          });
        }
      } catch {
        // Fallback: let the form submit normally
        form.submit();
      }
    });
  }

  initContact();
  document.addEventListener("astro:after-swap", initContact);
</script>
