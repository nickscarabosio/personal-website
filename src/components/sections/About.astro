---
import SectionLabel from "../ui/SectionLabel.astro";
---

<section
  id="about"
  class="relative mx-auto max-w-[var(--max-width)] px-6 md:px-10"
  style={`padding-top: var(--space-section); padding-bottom: var(--space-section);`}
>
  <div class="grid gap-12 md:grid-cols-2 md:gap-16">
    <!-- Left column -->
    <div class="about-left">
      <SectionLabel class="mb-4">About</SectionLabel>
      <h2
        class="font-heading text-[length:var(--text-3xl)] leading-tight text-[var(--color-text-primary)]"
      >
        I build things that<br />
        <span class="text-[var(--color-accent)]">move culture forward.</span>
      </h2>
    </div>

    <!-- Right column -->
    <div class="about-right flex flex-col justify-center gap-6">
      <p
        class="about-line text-[length:var(--text-base)] font-light leading-relaxed text-[var(--color-text-secondary)]"
      >
        I'm an operator, coach, and builder working at the intersection of
        culture, technology, and business. I help creators monetize their
        influence and build brands that last.
      </p>
      <p
        class="about-line text-[length:var(--text-base)] font-light leading-relaxed text-[var(--color-text-secondary)]"
      >
        Whether it's launching new ventures, advising startups as a fractional
        leader, or experimenting with new products — I'm always building.
      </p>
      <p
        class="about-line text-[length:var(--text-base)] font-light leading-relaxed text-[var(--color-text-secondary)]"
      >
        Based in the real world, shipping from everywhere.
      </p>
    </div>
  </div>
</section>

<script>
  import gsap from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  function initAbout() {
    gsap.fromTo(
      ".about-left",
      { opacity: 0, x: -30 },
      {
        opacity: 1,
        x: 0,
        duration: 0.8,
        ease: "power2.out",
        scrollTrigger: {
          trigger: "#about",
          start: "top 80%",
          once: true,
        },
      }
    );

    gsap.fromTo(
      ".about-line",
      { opacity: 0, y: 25 },
      {
        opacity: 1,
        y: 0,
        duration: 0.6,
        stagger: 0.12,
        ease: "power2.out",
        scrollTrigger: {
          trigger: ".about-right",
          start: "top 80%",
          once: true,
        },
      }
    );
  }

  initAbout();
  document.addEventListener("astro:after-swap", initAbout);
</script>
